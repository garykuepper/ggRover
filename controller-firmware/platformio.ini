[env:sparkfun_promicro8]
platform = atmelavr
board = sparkfun_promicro8
framework = arduino

lib_deps =
    adafruit/Adafruit SSD1306@^2.5.9
    adafruit/Adafruit GFX Library@^1.11.9
    # Note: Gamepad_PS4BT (Hobbytronics USB Host) may need manual installation
    # or specific library reference if available on PlatformIO.
    # Assuming standard I2C communication with USB Host adapter.

build_flags =
    -I ../shared
